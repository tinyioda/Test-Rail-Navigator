apiVersion: v1
kind: Service
metadata:
  name: {{ include "testrail-navigator.fullname" . }}
  labels:
    {{- include "testrail-navigator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "testrail-navigator.selectorLabels" . | nindent 4 }}
